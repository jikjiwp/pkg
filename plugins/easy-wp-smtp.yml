package:
  name: wp-easy-wp-smtp
  version: 2.6.0
  epoch: 0
  description: "Easy WP SMTP – WordPress SMTP and Email Logs: Gmail, Office 365, Outlook, Custom SMTP, and more"
  target-architecture:
    - aarch64
    - x86_64
  copyright:
    - paths:
      - "*"
      attestation: TODO
      license: TODO
  dependencies:
    runtime:
      - wordpress

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - unzip

pipeline:
  - uses: fetch
    with:
      uri: https://downloads.wordpress.org/plugin/easy-wp-smtp.2.6.0.zip
      expected-sha256: 05c6d08add96c88a6205c9f32b39f45027ff17fa120fecc9c332ffa3c9ac7ef5
      extract: false
  - runs: |
      mkdir -p ${{targets.destdir}}/usr/src/wordpress/wp-content/plugins/
      unzip easy-wp-smtp.2.6.0.zip -d ${{targets.destdir}}/usr/src/wordpress/wp-content/plugins/
